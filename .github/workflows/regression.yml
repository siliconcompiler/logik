---

name: Regression
on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - main

# The repository default is write-all; nothing here writes.
permissions:
  contents: read

jobs:
  version-compatibility:
    name: Test Python ${{ matrix.python-version }} with ${{ matrix.resolution }} deps

    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.14"]
        resolution: ["highest", "lowest-direct"]

    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v7
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install uv
        uses: astral-sh/setup-uv@v7

      - name: Install dependencies with ${{ matrix.resolution }} resolution
        run: |
          uv pip install --system --resolution ${{ matrix.resolution }} -e .[test]

      - name: Verify installation and imports
        run: |
          python -c "import logik; import siliconcompiler; print('✓ Imports successful')"
          python -c "from logik.flows.logik_flow import LogikFlow; print('✓ LogikFlow imported')"

      - name: Run basic tests (no CAD tools)
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          pytest tests/examples/test_files.py -v

  pytest:
    name: Run CAD flow tests

    runs-on: ubuntu-latest
    container:
      image: ghcr.io/siliconcompiler/sc_runner:latest

    env:
      # Every dependency logik resolves is public (logiklib and siliconcompiler
      # both come from PyPI, lambdapdk data from public repos), so the
      # repo-scoped GITHUB_TOKEN is enough. It buys authenticated API rate
      # limits, not private access. GIT_TOKEN is what SC's package fetcher
      # reads; GH_TOKEN is what gh reads.
      GIT_TOKEN: ${{ github.token }}
      GH_TOKEN: ${{ github.token }}

    steps:
      - uses: actions/checkout@v7

      - name: Setup permissions
        run: |
          apt update && apt install sudo -y
          type -p curl >/dev/null || (sudo apt update && sudo apt install curl -y)
          curl -fsSL https://cli.github.com/packages/githubcli-archive-keyring.gpg | sudo dd of=/usr/share/keyrings/githubcli-archive-keyring.gpg \
          && sudo chmod go+r /usr/share/keyrings/githubcli-archive-keyring.gpg \
          && echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/githubcli-archive-keyring.gpg] https://cli.github.com/packages stable main" | sudo tee /etc/apt/sources.list.d/github-cli.list > /dev/null \
          && sudo apt update \
          && sudo apt install gh -y

          gh auth setup-git --hostname github.com

      - name: Setup git access
        run: |
          git config --global --add url."https://github.com/".insteadOf git@github.com:
          git config --global --add safe.directory "$GITHUB_WORKSPACE"

      - name: Setup python
        run: |
          python3 -m venv --system-site-packages .logik
          . .logik/bin/activate
          python3 -m pip install --upgrade pip
          pip3 install -e .[test]

      - name: Run tests
        run: |
          . .logik/bin/activate
          pytest
